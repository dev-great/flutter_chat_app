import 'package:flutter/material.dart';

class CreatPostContainer extends StatelessWidget {
  const CreatPostContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
